library flazy;

import 'package:flutter/material.dart';

class UB extends StatelessWidget {
  const UB({
    super.key,
  });
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}

/// A Calculator.
class Calculator {
  /// Returns [value] plus 1.
  int addOne(int value) => value + 1;
}
